[
  {
    "function": "absolute.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "absolute.c",
    "claim": "output is always smaller than input",
    "expected": "FALSIFIED",
    "human_check": "if (result >= x) { exit(1); }"
  },
  {
    "function": "max.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "max.c",
    "claim": "output is always smaller than input",
    "expected": "FALSIFIED",
    "human_check": "if (result >= x) { exit(1); }"
  },
  {
    "function": "double.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "double.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "clamp.c",
    "claim": "result is never greater than 100",
    "expected": "VERIFIED",
    "human_check": "if (result > 100) { exit(1); }"
  },
  {
    "function": "clamp.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "min.c",
    "claim": "never returns more than 42",
    "expected": "VERIFIED",
    "human_check": "if (result > 42) { exit(1); }"
  },
  {
    "function": "min.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "increment.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "increment.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "identity.c",
    "claim": "output equals input",
    "expected": "VERIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "identity.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "zero.c",
    "claim": "always returns zero",
    "expected": "VERIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "zero.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "square.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "square.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "isZero.c",
    "claim": "output is always smaller than input",
    "expected": "FALSIFIED",
    "human_check": "if (result >= x) { exit(1); }"
  },
  {
    "function": "isZero.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "halve.c",
    "claim": "output is always smaller than input",
    "expected": "FALSIFIED",
    "human_check": "if (result >= x) { exit(1); }"
  },
  {
    "function": "halve.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "add10.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "add10.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "triple.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "triple.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "pred.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "pred.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "const42.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "const42.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "mod2.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "mod2.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "max_zero.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "max_zero.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "clamp10.c",
    "claim": "result is never greater than 10",
    "expected": "VERIFIED",
    "human_check": "if (result > 10) { exit(1); }"
  },
  {
    "function": "clamp10.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "pred.c",
    "claim": "output is always smaller than input",
    "expected": "FALSIFIED",
    "human_check": "if (result >= x) { exit(1); }"
  },
  {
    "function": "const42.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  }
  ,{
    "function": "between.c",
    "claim": "never returns more than 1",
    "expected": "VERIFIED",
    "human_check": "if (result > 1) { exit(1); }"
  },
  {
    "function": "between.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "positive.c",
    "claim": "never returns more than 1",
    "expected": "VERIFIED",
    "human_check": "if (result > 1) { exit(1); }"
  },
  {
    "function": "positive.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "clamp50.c",
    "claim": "result is never greater than 50",
    "expected": "VERIFIED",
    "human_check": "if (result > 50) { exit(1); }"
  },
  {
    "function": "clamp50.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "add100.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "add100.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "divby3.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "divby3.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "mod10.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "mod10.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "const0.c",
    "claim": "always returns zero",
    "expected": "VERIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "const0.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "const1.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "const1.c",
    "claim": "always returns zero",
    "expected": "FALSIFIED",
    "human_check": "if (result != 0) { exit(1); }"
  },
  {
    "function": "add1.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "add1.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "max100.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "max100.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  },
  {
    "function": "sign.c",
    "claim": "never returns more than 1",
    "expected": "VERIFIED",
    "human_check": "if (result > 1) { exit(1); }"
  },
  {
    "function": "sign.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  }
  ,{
    "function": "factorial.c",
    "claim": "never returns a negative value",
    "expected": "VERIFIED",
    "human_check": "if (result < 0) { exit(1); }"
  },
  {
    "function": "factorial.c",
    "claim": "output equals input",
    "expected": "FALSIFIED",
    "human_check": "if (result != x) { exit(1); }"
  }
]